# config for travis-ci
language: python
sudo: false
python:
  - "2.6"
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"
install:
  # gcut version
  - "pip install -v ."
script: true
cache:
- directories:
  - "$HOME/.cache/pip"
